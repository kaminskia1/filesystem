{% extends 'base.html.twig' %}

{% block title %}Folder{% endblock %}

{% block body %}
    {{ form_start(form, {'action': url('site_folder_add', {parentUuid: parent}) }) }}
    {{ form_errors(form) }}
    {{ form_widget(form.name, {'attr': {'placeholder': 'Name'}}) }}
    {{ form_end(form) }}
{% endblock %}
