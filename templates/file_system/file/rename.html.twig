{% extends 'base.html.twig' %}

{% block title %}File{% endblock %}

{% block body %}
    {{ form_start(form, {'action': url('site_file_rename', {uuid: fileUuid}) }) }}
    {{ form_errors(form) }}
    {{ form_widget(form.name, {'attr': {'placeholder': 'New Name'}}) }}
    {{ form_end(form) }}
{% endblock %}
