{% extends 'base.html.twig' %}

{% block title %}Folder{% endblock %}

{% block body %}
    {{ form_start(form, {'action': url('site_file_delete', {uuid: folderUuid}) }) }}
    {{ form_errors(form) }}
    {{ form_widget(form.type) }}
    {{ form_end(form) }}
{% endblock %}
