{% extends 'base.html.twig' %}

{% block title %}File{% endblock %}

{% block body %}
    {{ form_start(form, {'action': url('site_add_file', {parentUuid: parent}) }) }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_end(form) }}
{% endblock %}
