{% extends 'base.html.twig' %}

{% block title %}Folder{% endblock %}

{% block body %}
    {{ form_start(form, {'action': url('site_add_folder', {parentUuid: parent}) }) }}
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_end(form) }}
{% endblock %}
